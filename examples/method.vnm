struct person {
  name;
}

impl person {
  fn greet(self, prefix, suffix) {
    print prefix ++ self.name ++ suffix;
    return 0;
  }
}

fn main() {
  let p = person { name: "Joe" };
  let prefix = "Hello, ";
  let suffix = ".";
  p.greet(prefix, suffix);
  return 0;
}

main();