struct lolwoot {
    x;
}

fn add_one(func) {
    let s = lolwoot { x: 128 };
    let z = 32;
    fn inner(x) {
        return func(x + s.x) + 1;
    }
    return inner;
}

fn spam(x) {
    return x;
}

let spam = add_one(spam);

print spam(1);