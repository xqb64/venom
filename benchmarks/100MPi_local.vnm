fn exec(n) {
    let sum = 0;
    let flip = -1;
    let i = 1;
    while (i < n) {
        flip *= -1;
        sum += flip / (2 * i - 1);
        i += 1;
    }
    return sum;
}

let sum = exec(100000000) * 4;
print sum;